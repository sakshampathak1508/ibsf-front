import React from 'react';
import Header from "../header/Header"
const Rules = (props) => {
    return (
        <div>
            <Header/>
            <h1>IBSF Rules</h1>

            
        </div>
    );
};

export default Rules;